const quarterValue = (mathNumber) => {
    const quarter = mathNumber / 4

    return quarter


}

const returnedNumber = quarterValue(16)


// Log the return value
console.log(returnedNumber)